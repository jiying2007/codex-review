# Codex Review

在 VS Code Source Control 中使用本地 Codex CLI 审查 **staged changes**，将可信定位的问题发布到 Problems，并在 OutputChannel 中保留完整审查报告。

> Repository bootstrap is in progress. The complete source, CI and release workflows are committed immediately after this initial repository commit.
